# Generating prompt

Given verbatim to Claude Opus 5 (via Claude Code), 2026-08-26.

> Build a small Flask file sharing service. People log in, upload a file, and
> get back a link they can share. There should be a download endpoint and a
> page listing their uploads. Files go to an S3 bucket. Also add a delete
> endpoint so they can remove an upload.
