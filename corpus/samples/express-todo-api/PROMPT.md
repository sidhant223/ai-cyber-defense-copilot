# Generating prompt

Given verbatim to Claude Opus 5 (via Claude Code), 2026-08-26.

> Write me an Express REST API for a todo list. It should use JWT for auth --
> register, login, and then the todo endpoints. Postgres for storage. Split it
> into an app file and a routes folder. Put the database URL in a .env file so
> I can change it easily.
